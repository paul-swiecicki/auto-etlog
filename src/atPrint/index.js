const { clickCloseBtn, clickPrintBtns } = require("./clickBtns");
const { fillSerial } = require("./fillSerial");
const { fillTextInputs } = require("./fillTextInputs");
const { replaceAmount } = require("./replaceAmount");

module.exports = {
  clickPrintBtns,
  clickCloseBtn,
  fillSerial,
  fillTextInputs,
  replaceAmount,
};
